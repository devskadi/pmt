# Scorecard Model
# ---------------
# Fields: id, project_id (FK), user_id (FK),
#         sprint_id (FK, nullable), criteria (JSON —
#         configurable weight definitions), total_score,
#         evaluated_by (FK), evaluated_at,
#         created_at, updated_at
#
# Relationships: project, user, evaluator, sprint
#
# Placeholder — implementation pending.
