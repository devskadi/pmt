# Notification Model
# ------------------
# Fields: id, user_id (FK), title, message,
#         type (enum: TASK_ASSIGNED/COMMENT/SPRINT_START/
#         MENTION/SYSTEM), is_read, entity_type,
#         entity_id, created_at
#
# Placeholder — implementation pending.
