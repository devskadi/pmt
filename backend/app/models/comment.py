# Comment Model
# -------------
# Fields: id, content, task_id (FK), author_id (FK),
#         parent_id (FK, nullable — threaded replies),
#         created_at, updated_at
#
# Relationships: task, author, parent, replies
#
# Placeholder — implementation pending.
