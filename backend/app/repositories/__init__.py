# Repository Layer
# ----------------
# Data access abstraction. Each repository encapsulates
# SQLAlchemy queries for a single aggregate root.
# Services compose repositories — never access session directly.
