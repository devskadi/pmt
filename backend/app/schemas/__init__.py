# Pydantic v2 Schemas
# -------------------
# Request/response models organized by domain entity.
# Each module follows the pattern:
#   - EntityBase (shared fields)
#   - EntityCreate (input for creation)
#   - EntityUpdate (partial update)
#   - EntityRead / EntityResponse (API output)
#   - EntityFilter (query parameters)
