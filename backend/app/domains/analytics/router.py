# Analytics Router — /api/v1/analytics
# --------------------------------------
# Endpoints:
#   GET /dashboard           — Dashboard summary metrics
#   GET /projects/{id}/stats — Project-level statistics
#   GET /sprints/{id}/report — Sprint burndown/velocity report
#   GET /users/{id}/activity — User activity summary
#   GET /activity-log        — Activity feed (cursor-based pagination)
#
# All analytics endpoints are read-only.
#
# Placeholder — implementation pending.
