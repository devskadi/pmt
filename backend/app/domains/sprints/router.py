# Sprints Router — /api/v1/projects/{project_id}/sprints
# -------------------------------------------------------
# Endpoints:
#   POST   /                  — Create sprint
#   GET    /                  — List sprints for project
#   GET    /{id}              — Get sprint details
#   PATCH  /{id}              — Update sprint
#   POST   /{id}/start        — Start sprint
#   POST   /{id}/complete     — Complete sprint
#   DELETE /{id}              — Delete sprint (only if PLANNING status)
#   GET    /{id}/tasks        — List tasks in sprint
#
# Placeholder — implementation pending.
