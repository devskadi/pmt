# Sprint Schemas
# --------------
# Pydantic v2 schemas for the sprints domain:
#
#   SprintCreate   — name, goal?, start_date?, end_date?
#   SprintUpdate   — name?, goal?, start_date?, end_date?
#   SprintRead     — id, project_id, name, goal, status, start_date, end_date, task_count, created_at
#   SprintSummary  — id, name, status (for embedding)
#
# Placeholder — implementation pending.
