# Scorecards Router — /api/v1/scorecards
# ----------------------------------------
# Endpoints:
#   POST   /              — Create scorecard evaluation
#   GET    /              — List scorecards (filtered by project/sprint/user)
#   GET    /{id}          — Get scorecard details
#   PATCH  /{id}          — Update scorecard
#   DELETE /{id}          — Delete scorecard
#
# Placeholder — implementation pending.
