# Projects Router — /api/v1/projects
# ------------------------------------
# Endpoints:
#   POST   /                     — Create project
#   GET    /                     — List projects (filtered by membership)
#   GET    /{id}                 — Get project details
#   PATCH  /{id}                 — Update project
#   DELETE /{id}                 — Archive project (soft delete)
#   POST   /{id}/members         — Add member to project
#   DELETE /{id}/members/{uid}   — Remove member from project
#   PATCH  /{id}/members/{uid}   — Update member role in project
#   GET    /{id}/members         — List project members
#
# Placeholder — implementation pending.
