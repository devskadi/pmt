# Notifications Domain
# --------------------
# Owns: notification entity, delivery channels, read/unread state.
# Extraction candidate: Phase 4 (WebSocket service).
# Boundary: Consumes domain events — never queries other domains directly.
