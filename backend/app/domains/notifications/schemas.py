# Notification Schemas
# --------------------
# Pydantic v2 schemas for the notifications domain:
#
#   NotificationRead   — id, type, title, body, entity_type, entity_id, is_read, created_at
#   UnreadCount        — count: int
#
# Placeholder — implementation pending.
