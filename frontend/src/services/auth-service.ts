/* Auth API Service */

// TODO: Implement with apiClient
export const authService = {
  login: async (email: string, password: string) => null,
  register: async (data: Record<string, unknown>) => null,
  logout: async () => {},
  refreshToken: async () => null,
};
