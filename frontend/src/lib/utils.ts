/* Utility Functions
 * -----------------
 * cn() — clsx + tailwind-merge combo
 * formatDate(), formatCurrency(), truncate(),
 * sleep(), debounce(), etc.
 *
 * Placeholder — implementation pending.
 */
